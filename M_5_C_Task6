#include <stdio.h>

int main ()
{
char a[] = {0xAA, 0xAA, 0xAA, 0xAA, '\0'};

printf("Result in float is %f\n", a);
printf("Result in signed int is %d\n", a);
printf("Result in unsigned int is %u\n", a);

return 0;
}
