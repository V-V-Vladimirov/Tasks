#include<stdio.h>
#include<string.h>

int main ()
{
  char str[] = "Hello";
  unsigned int i = 0;
  while (i<strlen(str)) 
  {
      if (str[i]=='l') str[i]='x';
      i++;
  }
  printf("%s", str);
  return 0;
}
